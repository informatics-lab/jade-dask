# Deployment
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: dask-workers
  namespace: dask
spec:
  replicas: 0
  template:
    metadata:
      labels:
        app: dask-workers
    spec:
      containers:
        - image: daskdev/dask:latest
          command: ["sh", "-c"]
          args:
            - dask-worker $DASK_SCHEDULER_SERVICE_HOST:$DASK_SCHEDULER_SERVICE_PORT --nprocs 1 --nthreads 1 --host $POD_IP --memory-limit  4.295e+9 --name $POD_NAME
          imagePullPolicy: Always
          name: dask-worker
          env:
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          resources:
            requests:
              cpu: 1000m
              memory: 4Gi
            limits:
              memory: 4Gi
              cpu: 1000m
---
apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: no-worker-disruption
spec:
  selector:
    matchLabels:
      app: dask-workers
  minAvailable: 100%
